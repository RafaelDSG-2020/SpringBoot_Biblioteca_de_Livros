CREATE TABLE BOOK(
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    TITLE VARCHAR2(50) NOT NULL,
    PUBLISHING_COMPANY VARCHAR2(50) NOT NULL,
    PUBLISHING_DATE VARCHAR2(50) NOT NULL,
    ISBN VARCHAR2(50) NOT NULL,
    PRIMARY KEY(ID),
    UNIQUE (ISBN)
    );